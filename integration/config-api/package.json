{
  "name": "integration-config-api",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "json-server": "0.17"
  },
  "scripts": {
    "start": "json-server --host 0.0.0.0 --read-only --routes routes.json config.json"
  }
}
